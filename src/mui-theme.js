import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    background: {
      dark: "#DDDDDD",
      default: "#f6f6f8",
      paper: "#f6f6f8",
    },
  },
});

export default theme;
